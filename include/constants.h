#pragma once

// Game Window
inline constexpr int windowWidth = 500;
inline constexpr int windowHeight = 620;
inline constexpr int targetFPS = 60;

inline constexpr int floorY = 570;

inline constexpr float gravity = 0.4f;
